{
  "name": "maher-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public/pictures && cp index.html public/index.html && (cp \"RESUME MAHER .pdf\" public/ || true) && (cp -r pictures/. public/pictures/ 2>/dev/null || :) && esbuild index.tsx --bundle --outfile=public/bundle.js --jsx=automatic --format=esm --external:react --external:react-dom/*"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
